## 데이터 파악
- history → 6개월치 재처리 전용 → 문서
- static → 전체 정적 (스냅샷) → 레벨밖에없는 해머데이터
- 한가지, 주의할점은 history 의 경우 문서/채널 데이터에 hammer level 이 join 되어 있는 형태의 원본입니다
- 6개월치 input/???/history 에 쌓이는 데이터는 STRAW(원본) -> WASP -> KAFKA -> HDFS  →>> level 값은 이미 history 데이터에 join 되어 있습니다..
- 그리고, `{cp}_hammer` 는 매주 재처리를 위해서 dump 받은 hammer 데이터 입니다.
- 최종레벨 externals
### Hadoop Web Dynamic


### Hadoop Blog Dynamic
- d

### Hadoop Cafe Dynamic


### ETC
- mysql?


palu-runner kafka url 변경필요한 서비스
- blog (crawler, tistory, brunch2)
    - **blog_dynamic** - receiver_output, extract_input
    - **blog_hure** - receiver_output, extract_input
    - **blog_pump** - extract_input
- cafe
    - **cafe_dynamic** - extract_input, extract_output
    - **cafe_pump** - extract_input
    - **cafe_hure** - extract_input
- cafe_comment
    - **cafe_comment_dynamic** - extract_input
- news
    - **news_dynamic** - extract_input
- kas
    - kas_dynamic - extract_input
    - kas_pump - extract_input
    - kas_hure - extract_input
- table
    - table_dynamic - extract_input
    - table_pump - extract_input
- trend_tagging
    - web_trend_tagging_dynamic - extract_input
    - news_trend_tagging_dynamic - extract_input
    - cafe_trend_tagging - extract_input
- video
    - bifrost_dynamic - extract_input