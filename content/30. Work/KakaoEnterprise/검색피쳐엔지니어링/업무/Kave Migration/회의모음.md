## 25.01.02 10:00am
steve, ziggy, john, cypher
장소: 4층 진저

### rehao to kave 변경사항
- Gateway → Kave Connector
	- 응답 포맷 99% 동일하나
	- 페이징 방법이 달라서 변경해야함

### kave 사용법
- 프로젝트 생성 후 → 컬렉션 생성 (key unique 하지 않음) → 컬렉션 설정 → 
- (kave >이용안내) → 메뉴얼
- 컬렉션 설정
	- 컬렉션 ID에 버저닝까지 하는게 일반적
	- 기본정보 설정
		- Breeze 버전은 5부터 사용
		- DHA default
		- DHA 버전 2.9
	- 엔진 스키마 설정
		- Breeze가 XML 기반이라 rehao에서 복붙 후 검증 필요
		- UX로 설정가능
	- 저장소/색인
		- AOG를 찔러서 pull 방식으로 가져옴
		- PP 전체 색인할 경우 80G
		- 정적 색인 seed는 720개
		- 정적색인 sourcer
			- 서비스 기준으로 동적 DS는 rehao와 동일하지만, 정적 DS는 Sourcer를 사용해야함 (버그가 있었음)
			- Parallel Fetch Per Sourcer → 하나의 Sourcer당 병렬로 몇개를 Fetch 할건가? (스레드갯수)
			- 고려해야할 부분: 몇개의 서비스를 할건가?
	- 엔진교체검증
		- 검색 결과 비교: cypher도 확인이 필요함
		- 문서 수 비교 off로 설정
	- 앱 (앱 관리 콘솔창)
		- 인증 키값을 header에 넣음
	- 검색 프리셋
		- 검색 파라미터 프리셋
	- 엔진 리소스
		- Sourcer, 클러스터등 리소스를 조정하는 부분
		- 색인 하면서 리소스 조정이 필요 (SA)
		- 정적 색인 Sourcer 
		- 예) AOG가 40개 seed를 처리하고있을때는 Sourcer를 10개로 지정
		- Sourcer개수를 늘리는것보다 Parallel Fetch를 늘리는게 좋을거같지만 테스트해봐야함.
			- 설정 부분에 대한 가이드는 없는지? → 아직없음
			- 문서 개수, 문서 사이즈 별로 어떻게 Sourcer 리소스 (CPU,MEM)를 조정해야할지 가이드가 있으면 좋겠음.
			- cypher: 색인을 해보면 초기 클러스터 설정과 다르게 동적으로 바뀔 가능성도 있음 (확실하지않음)
				- 예) 데이터가 SA 1G에 포함되지 않으면 자동 scale-out 될수도?
			- 클러스터 == 샤딩
		- 해결방안: 현재 가이드는 존재하지 않으므로 일단 색인을 해보고, error log 보면서 커뮤니케이션 하는걸로.
	- 고급
		- rehao 때처럼 conf 파일 관리 할 수 있음
		- 그대로 가져오면 에러가 나는 부분이 있음 (breeze 3 → breeze 5)
			- 확인해보니 pp는 문제 없을거같음.
			- conf 저장후 → 즉시 색인 클릭.


### kave 구축 고려사항 및 차이점
cyper 권장
- 64 Cluster
- 12G SA Memory
- 예전에는 rehao 320 cluster였음. gateway timeout 3초가 max였지만 지금은 응답 3초 이상도 결과가 안나오는 case없음.
steve: cluster를 128개로 늘리고 메모리를 줄이면 response가 빨라지나요?
- yes. pm k8s여서 응답은 빨리지지만 리소스를 많이씀.


|                 | REHAO                               | KAVE                                                  |     |
| :-------------- | :---------------------------------- | :---------------------------------------------------- | :-- |
| 검색 parameter 설정 | page_num, page_size                 | offset, limit                                         |     |
| 검색 타임아웃         | Gateway 3초 타임아웃 한계, 검색 타임아웃을 3초로 정의 |                                                       |     |
| 색인 클러스터         | 기존 320 cluster                      | 64 Cluster, N Cluster                                 |     |
| 정적색인 병렬처리량      | 최대 동접 40                            | 최대 동적 N (Parallel fetch per sourcer * sourcer 서비스 갯수) |     |
| AOG 정적 seed     | 720 시드                              | N개의 시드로 최적화                                           |     |
