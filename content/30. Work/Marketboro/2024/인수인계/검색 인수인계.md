# 인프라 인수인계 (07.01)
## Overview
시스템 구조 & 인프라 간소화
인프라 변경 (DA랩 → WCC실)
식봄 개발팀의 개발 프로세스 적용
Airflow 제거
Python → Java
ECS 사용
Domain 변경 (식봄 도메인으로)

색인 주기 개선
최대한 실시간으로 (목표 1분이내)
검색로직 변경
검색 매칭결과 정확성 개선
API 재설계
신규 기능 추가
검색어 자동완성
검색필터
한영 단어 전환
동의어 검색

## Monitoring

### Access-log 보는법
- 초당 TPS가 2.75 나온다.
- 하지만 식봄 사이트에서는 한번 검색할때 요청이 4개가 날라가서 사실상 초당 한번 검색 할까말까 정도다.

### ElasticAlert
- DEVOPS한테 이관하기

### Logback 보는법
로그 용량이 30-50G이 정상이다. 추천 + 검색이 들어가서 많다.

- logback-12 : port 8032
- logback-14 : port 8034

시작하는법
- jar 3개 필요함
```bash
./logback-server.sh start
```



### Fluntd
```python
vi /etc/fluent/fluentd.conf
```


# 검색 로직 인수인계 (07.02)
## rmcd-serving
### gradle
v2: low-level client → EsRestClientConfig.java
v1: high-level client → 

### 분기 되는 시점
- V1 Controller에서 v1, v2 로직 분기
- V1 Controller 에서 shop_id 분기

### v1_default
- v2_default 로 바꿀려면
- FsSearchControllerV1.java , SearchVersionSelector.java enum 수정

### search_log (파일이 커지고있다…)


# 추천 로직 인수인계 (07.03)
## 기본 구조
유저가 random 인지 cuisine model 사용할건지 selector 가 구분
모델에 따라 goods_id 가져오고, 상품 정보를 DB에서 가져온 다음 조인.
유저가 추천상품을 조회할 때마다 상품 정보를 db에서 가져온다음 조인해야하기 때문에 헤비하다.
## 문제가 될만한 부분
