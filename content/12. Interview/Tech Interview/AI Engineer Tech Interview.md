---
title: AI Engineer Tech Interview
date: 2025-09-21
draft: false
tags:
  - MachineLearning
  - DeepLearning
complete: true
banner: /_assets/_img/_banners/nebula.jpg
link: https://github.com/boost-devs/ai-tech-interview
link2: https://boostdevs.gitbook.io/ai-tech-interview/interview/2-machine-learning
---
# **Basic but Effective (Open Question)**
### 딥러닝의 장점과 단점을 말해주세요

### 오버피팅은 어떻게 확인하나요?
#### Over-fitting 의 대응 기법들을 설명해주세요.

### 피쳐의 선택을 어떤기준으로 하실건가요?

### 경사하강법

### Decision Tree vs GBM vs XGBoost


---

# **Machine Learning**

## Metric
### 알고 있는 metric에 대해 설명해주세요. (ex. RMSE, MAE, recall, precision ...) #⭐ 

#### 회귀 / 분류시 알맞은 metric은 무엇일까?
회귀 문제에서는 모델이 예측한 값이 실제 값과 얼마나 차이나는지를 보는 지표들을 씁니다. 대표적으로 **MSE, MAE, RMSE**가 있고요. MSE는 오차를 제곱해서 평균 낸 거라 이상치에 민감하고, MAE는 절댓값 평균이라 직관적이지만 데이터 크기에 영향을 많이 받아요. 그래서 보완 지표로 **R²**라는 결정계수를 쓰는데, 1에 가까울수록 모델 설명력이 높다고 해석할 수 있습니다.

분류 문제에서는 Confusion Matrix 기반 지표를 씁니다. 가장 기본은 정확도(Accuracy)인데 클래스가 불균형할 땐 신뢰하기 어려워요. 그래서 정밀도(Precision)랑 재현율(Recall)을 많이 봅니다. 정밀도는 모델이 P라고 한 것 중에 진짜 P의 비율이고, 재현율은 실제 P 중에 모델이 잘 잡아낸 비율이에요. 예를 들어 스팸 메일 필터는 정밀도가 중요하고, 암 진단 같은 건 재현율이 더 중요하죠. 이 둘의 균형을 본 게 F1 Score고요.

또 **ROC Curve**와 **AUC**도 많이 씁니다. ROC는 FPR을 x축, TPR을 y축으로 둔 그래프고, 그 면적 AUC 값이 높을수록 좋은 모델입니다.

정리하면, 회귀는 MSE·MAE·R² 같은 오차 기반 지표를, 분류는 Accuracy, Precision, Recall, F1, ROC/AUC 같은 혼동 행렬 기반 지표를 상황에 맞게 선택해서 씁니다.

하지만 **상황에 따라 선정해야 하는 평가 지표가 다르므로 다양한 평가 지표를 적용 후 결과를 비교하는 것이 필요**합니다.

> [!NOTE]- ***Detail***
> 모델에게 주어진 목표를 얼마나 잘 달성했는가를 측정해야 어떤 모델이 좋은 모델인지 알 수 있습니다.
> 
> 회귀 문제
> 
> 회귀 문제에서는 실제 값과 모델이 예측하는 값의 차이에 기반을 둔 metric(평가)을 사용합니다. 대표적으로 **RSS**(단순 오차 제곱 합), **MSE**(평균 제곱 오차), **MAE**(평균 절대값 오차)가 있습니다. RSS는 **예측값과 실제값의 오차의 제곱합**, MSE는 **RSS를 데이터의 개수만큼 나눈 값**, MAE는 **예측값과 실제값의 오차의 절대값의 평균**입니다. 그리고 **RMSE**와 **RMAE**라는 것도 있는데, 각각 MSE와 MAE에 루트를 씌운 값입니다.
> 
> MSE의 경우 오차에 제곱이 되기 때문에 **이상치(outlier)를 잡아내는 데 효과적**입니다. 틀린 걸 더 많이 틀렸다고 알려주는 것입니다. MAE의 경우 **변동치가 큰 지표와 낮은 지표를 같이 예측하는 데 효과적**입니다. 둘 다 가장 간단한 평가 방법으로 직관적인 해석이 가능하지만, 평균을 그대로 이용하기 때문에 **데이터의 크기에 의존한다는 단점**이 있습니다.
> 
> 예를 들어 현재 삼성전자의 주가가 200만이고 네이버의 주가가 70만일 때, 각각의 주가를 예측하는 회귀 모델을 만들었다고 생각해봅시다. 이때 삼성전자의 MSE가 50만이 나오고 네이버의 MSE도 50만이 나왔습니다. 그러면 각 기업의 주가를 예측하는 모델의 성능은 같은 걸까요? 아닙니다. 상대적으로 네이버의 경우가 더 모델이 안 좋다고 할 수 있습니다. 따라서 MSE는 전체 데이터의 크기에 의존하기 때문에 서로 다른 두 모델의 MSE만을 비교해서 어떤게 더 좋은 모델인지 판단하기 어렵다는 단점이 있습니다.
> 
> 이를 해결하기 위한 metric으로는 **R2**(결정계수)가 있습니다. R2는 **1-(RSS/전체 분산)**입니다. R2는 **회귀 모델의 설명력을 표현**하는 지표로써, 그 값이 1에 가까울수록 높은 성능의 모델이라고 할 수 있습니다. R2의 식에서 분자인 RSS의 근본은 실제값과 예측값의 차이인데, 그 값이 0에 가까울수록 모델이 잘 예측을 했다는 뜻이므로 R2값이 1에 가까워지게 됩니다.
> 
> 분류 문제
> 
> 분류 문제에서는 어떤 모델이 얼마나 데이터를 클래스에 잘 맞게 분류했느냐를 측정하기 위해서 **혼동 행렬**(Confusion Matrix)라는 것에서 나오는 지표를 사용합니다. 혼동 행렬은 어떤 데이터에 대해 모델이 예측한 클래스와 실제 클래스가 일치하느냐 아니냐를 따지게 됩니다.
> 
> ![](https://i.imgur.com/RexoocJ.png)
> 
> True Positive(TP)는 실제 데이터가 P(긍정)인데 모델도 P라고 잘 예측했을 때,
> True Negative(TN)는 실제 데이터가 N(부정)인데 모델도 N이라고 잘 예측했을 때,
> False Positive(FP)는 실제 데이터가 N인데 모델은 P라고 잘못 예측했을 때,
> False Negative(FN)는 실제 데이터가 P인데 모델은 N이라고 잘못 예측했을 때입니다.
> 
> 나의 얼굴 인식을 예로 한 번 들어보겠습니다. TP는 실제로 나의 얼굴이고 모델도 나의 얼굴이라고 인식했을 때입니다. TN은 실제로 다른 사람의 얼굴이고 모델도 다른 사람의 얼굴이라고 인식했을 때입니다. FP는 실제로 다른 사람의 얼굴이지만 모델은 나의 얼굴이라고 인식했을 때입니다. FN은 실제로 내 얼굴이지만 모델은 다른 사람의 얼굴이라고 인식했을 때입니다.
> 
> 가장 쉽게 생각할 수 있는 metric으로 **accuracy, 정확도**가 있습니다. **정확도는 (TP+TN) / (TP+TN+FP+FN)**으로 나타냅니다. 정확도는 **모델이 얼마나 데이터를 잘 분류했느냐**, 즉 분류 결과가 얼마나 True로 나왔느냐를 보여줍니다. 정확도는 일반적으로 분류 모델의 주요 평가 지표로 쓰이지만, 클래스의 개수가 불균형할 경우 신뢰도가 떨어진다는 단점이 있습니다.
> 
> 만약 100만 개의 항공 데이터 중 90만 개가 정상 운행을 했고 10만 개가 지연 운행을 했다는 데이터를 가지고 앞으로의 운행이 어떻게 될지를 예측한다고 해봅시다. 이때 분류 모델 A가 전체 결과가 모두 지연되지 않았다고 예측할 경우 정확도는 (0+90만)/(100만) = 90%가 됩니다. 데이터 분포가 어떻든 간에 그냥 지연되지 않을 거라고만 예측하면 정확도가 90%가 나오는데, 그렇다고 해서 이걸 좋은 모델이라고 할 수 있을까요? 아마 아닐 것입니다. 따라서 정확도를 대신하기 위해 나온 metric이 몇 가지 더 있습니다.
> 
> **Precision, 정밀도**라는 게 있습니다. 정밀도는 **‘모델이 P라고 분류한 데이터 중에서’ 실제로 P인 데이터의 비율**을 나타냅니다. 정밀도는 **N인 데이터가 더 중요할 경우**, 즉 실제로 N인 데이터를 P라고 판단하면 안 될 때 많이 사용하게 됩니다. 예를 들어 스팸 메일을 분류할 때, 일반 메일(P)을 스팸 메일(N)로 잘못 분류했을 땐 중요한 메일을 읽지 못하게 될 수 있습니다. 정밀도에선 ‘모델의 관점에서’ P라고 예측한 것이 분모로 들어가고, **TP/(TP+FP)**로 나타나게 됩니다. 분자로는 실제 P를 모델도 P라고 맞춘 것의 개수인 TP가 들어갑니다.
> 
> **Recall (또는 True Positive Rate, TPR), 재현율**이라는 게 있습니다. 재현율은 **‘실제 P인 데이터 중에서’ 모델이 P라고 잘 예측한 데이터의 비율**을 나타냅니다. 재현율은 **P인 데이터가 더 중요할 경우**, 즉 실제로 P인 데이터를 N이라고 판단하면 안 될 때 사용하게 됩니다. 예를 들어 종양의 종류를 판단할 때, 악성 종양(P)을 음성 종양(N)으로 잘못 분류하면 생명이 위급해질 수 있습니다. 재현율은 ‘데이터의 관점에서’ 실제로 P인 것이 분모로 들어갑니다. 따라서 재현율은 **TP/(TP+FN)**로 나타냅니다.
> 
> #### ROC 커브에 대해 설명해주실 수 있으신가요?
> 
> 또한 **False Positive Rate, FPR**이라는 게 있습니다. FPR은 **실제로 N인 데이터 중에서 모델이 P라고 잘못 분류한 데이터의 비율**입니다. 그래서 분모로는 실제로 N인 데이터가 들어가고 분자로는 모델이 P라고 잘못 분류한 데이터의 수가 들어가므로 FPR은 **FP/(FP+TN)**이 됩니다.
> 
> **x축을 FPR, y축을 TPR(Recall)**로 두고 이를 시각화한 그래프를 **ROC curve**라고 합니다. 그림을 참고하면 이해가 쉬울 것 같습니다. **ROC curve의 아래 면적을 AUC**라고 하는데, 이를 보고 모델의 성능을 평가합니다. 만약 ROC curve가 y=x선처럼 된다면 AUC는 (1*1)/2=0.5가 되고, 이는 분류기로써 최하 성능을 나타내는 것입니다. 만약 TPR, 즉 y축 쪽으로 그래프가 당겨진다면 좋은 쪽으로 그래프가 기우는 것이니까 분류기의 성능이 향상된다는 것을 의미합니다.
> 
> ![|550](https://i.imgur.com/p83PSWj.png)
> 
> #### F1-Score에 대해서 설명해주세요 (Precision/Recall 과 f1-score의 상관관계는 뭘까요?)
> **F1 score**라는 지표도 있습니다. F1 score는 정밀도와 재현율을 적절히 혼합해 사용하기 위하여 만들어진 지표로, 정밀도와 재현율의 가중 조화평균 값으로 구성되어 있습니다. 원래는 아래와 같은 F-measure라는 식이고, 여기에 alpha값으로 정확도와 재현율 중 어느 쪽에 더 가중치를 둘 것이냐를 결정합니다. F1 score는 alpha값이 0.5인 경우입니다.
> 
> ![](https://i.imgur.com/IwcIigo.png)
> 
> F-measure에서 왜 우리가 아는 산술평균을 쓰지 않고 굳이 조화평균을 쓰느냐? 그 이유는 **조화평균을 썼을 때 정밀도와 재현율 중 더 작은 값에 가깝게 그 결과를 던져주기 때문**입니다. 약간 안전빵(?) 같은 개념이라고도 볼 수 있겠네요. 예를 들어 정밀도(P)가 0.1, 재현율(R)이 0.9라고 할 때 F1 score를 계산해보면 0.18이 나오고, 이는 P에 더 가깝습니다. 정밀도(P)가 0.7, 재현율(R)이 0.3이라고 할 때 F1 score를 계산해보면 0.42가 나오고, 이는 R에 더 가깝습니다.
> 
> 지금까지 분류와 회귀 문제에서 쓰이는 다양한 평가 지표들에 대하여 설명해 드렸습니다. **상황에 따라 선정해야 하는 평가 지표가 다르므로 다양한 평가 지표를 적용 후 결과를 비교하는 것이 필요**합니다.

> [!NOTE]- 예시 문제:
> 창고의 지문 인식기 성능을 측정한다고 가정하겠습니다. 이때 **나의 지문**을 긍정(**Positive**) 데이터, **상대방의 지문**을 부정(**Negative**) 데이터로 정의하겠습니다.
> 
> 그리고 지문 인식기가 내 지문을 내 지문으로 잘 인식하고 창고가 열리는 경우, 또는 상대방 지문을 상대방 지문으로 잘 인식하고 창고가 열리지 않는 경우를 **True**라고 하겠습니다.
> 
> 반면에 지문 인식기가 상대방 지문을 내 지문으로 잘못 인식하고 창고가 열리는 경우, 또는 내 지문을 상대방 지문으로 잘못 인식하고 창고가 열리지 않는 경우를 **False**라고 하겠습니다.
> 
> 이러한 상황에서, 총 100번의 실험을 통해 지문 인식기의 정밀도와 재현율을 측정하고자 합니다. 실험 결과는 다음과 같습니다.
> 
> - 지문 인식기가 내 지문을 잘 인식하고 금고를 열었을 때 : 50번
> - 지문 인식기가 상대방 지문을 잘 인식하고 금고를 열지 않았을 때 : 40번
> - 지문 인식기가 상대방 지문을 내 지문으로 잘못 인식하고 금고를 열었을 때 : 2번
> - 지문 인식기가 내 지문을 상대방 지문으로 잘못 인식하고 금고를 열지 않았을 때 : 8번
> 
> 위와 같은 실험 결과, 지문 인식기의 **정밀도**와 **재현율**을 순서대로 올바르게 구하세요.
> 
> → 정밀도 = TP / TP + FP = 50 / 50 + 2 = 50/52
> → 재현율 = TP / TP + FN = 50 / 50 + 8 = 50/58


## 데이터 전처리 및 피처 표현

### 정규화 (regularization) 를 왜 해야할까요? 정규화의 방법은 무엇이 있나요? #⭐
정규화는 데이터의 스케일을 맞춰서 어떤 feature가 다른 feature를 지배하지 못하게 하는 과정입니다. 예를 들어 아파트 가격을 예측할 때, 나이는 5년, 연간 상승률은 0.1인데 가격은 1억이면, 모델이 가격만 중요하게 보고 나머지를 무시할 수 있어요.

정규화의 대표적인 방법은 두 가지가 있습니다.  
첫 번째는 **Min-Max 정규화**예요. 데이터를 0과 1 사이로 압축하는 방식인데, 계산식은 (X - MIN) / (MAX - MIN)입니다. 직관적이지만 이상치가 있으면 값들이 한쪽으로 몰린다는 단점이 있어요.

두 번째는 **Z-Score 정규화**예요. 평균을 0, 표준편차를 1로 맞추는 방식인데, 계산식은 (X - 평균)/표준편차입니다. 이 방법은 이상치에 덜 민감하고 데이터가 정규분포에 가까워지도록 만들어줍니다.

정리하면, 정규화는 모델이 데이터를 더 균형 있게 해석할 수 있도록 도와주는 전처리 단계라고 설명할 수 있습니다.


> [!NOTE]-  Detail
> 우리가 아무리 좋은 데이터를 갖고 있어도 모델에 학습시키기 위해선 **스케일링(Scaling)**을 해줘야 합니다. 머신러닝 모델은 데이터가 가진 feature(특징)을 뽑아서 학습합니다. 이때 **모델이 받아들이는 데이터의 크기가 들쑥날쑥하다면 모델이 데이터를 이상하게 해석할 우려**가 있습니다.
> 
> 예를 들어 아파트 가격을 예측한다고 합시다. 아파트 가격에 영향을 미치는 요소로는 지어진 지 몇 년이나 되었는지, 현재 가격은 얼마인지, 연 가격 상승률은 얼마인지 등등이 있을 것입니다. 각각을 5, 1억, 0.1이라고 칩시다. 그렇다면 모델이 세 가지의 feature를 보고 학습할 땐 5와 0.1은 무시하고 1억만 중요하게 볼 수도 있겠죠?
> 
> 저는 실제로 이러한 문제를 겪은 적이 있습니다. ‘kp지수 예측 경진대회’에 참여했을 때 많은 데이터 전처리가 필요했는데, 당시에 이러한 정규화를 할 생각을 못 해 모델의 성능이 그다지 좋지 않았던 기억이 있습니다. 예를 들면 300이라는 값을 갖는 feature와 0.5라는 값을 갖는 feature를 함께 쓴 꼴이었습니다.
> 
> 따라서 이런 문제가 발생하지 않도록 **모든 데이터가 같은 정도의 스케일(중요도)로 반영되도록 해주는 것이 정규화의 목표**입니다. 다시 말하면, **머신러닝 모델에 주입되는 데이터들을 균일하게 만드는 모든 방법**이라고 할 수 있습니다.
> 
> 이제 정규화 방법들을 소개해드리겠습니다.
> 
> Min-Max Normalization (최소-최대 정규화)
> 
> 먼저 Min-Max Normalization(최소-최대 정규화) 이 있습니다. 이 방법은 **모델에 투입될 모든 데이터 중에서 가장 작은 값을 0, 가장 큰 값을 1로 두고, 나머지 값들은 비율을 맞춰서 모두 0과 1 사이의 값으로 스케일링해주는 것**입니다.
> 
> 따라서 만약 X라는 값을 Min-Max Normalization 시킨다면 X는 **(X - MIN) / (MAX-MIN)**라는 값이 될 것입니다. 저 같은 경우는 태양 이미지를 처리할 때 각 이미지의 픽셀값에 대해 Min-Max Normalization을 시켜서 모델에 넣었던 기억이 납니다.
> 
> 하지만 Min-Max Normalization에는 **이상치(outlier)에 대해 취약**하다는 점이 있습니다. 예를 들어 100개의 값이 있는데 그중 99개는 0과 40 사이에 있고, 나머지 하나가 100이면 어떨까요? 그러면 99개의 값이 모두 0부터 0.4 사이의 값으로 변환되지만 단 하나뿐인 100은 1.0으로 변환됩니다. 결국, 데이터의 분포가 0부터 0.4 사이의 값들로 치우치게 됩니다. 만약 100개 모두 0과 40 사이에 있었다면 0은 0으로, 40은 1로 변환되어 좀 더 고른 형태의 데이터 분포를 기대할 수 있었을 겁니다.
> 
> Z-Score Normalization (Z-점수 정규화)
> 
> 이러한 문제를 해결할 수 있는 **Z-Score Normalization(Z-점수 정규화)**이라는 방법이 있습니다. 고등학교 통계 시간에 배웠던 **표준화 작업**을 한다고 보면 됩니다. 그래서 그런지 어떤 글에서는 Min-Max Normalization을 정규화, Z-Score Normalization을 표준화라고도 합니다. 여기선 그냥 둘 다 정규화의 기법들로 보고 설명하려고 합니다. 우리는 이 방법을 이용해 이상치에 해당하는 데이터의 문제를 피할 수 있습니다.
> 
> **Z = (X – 평균) / (표준편차)**를 통해 X라는 값을 Z라는 Z-점수로 바꿀 수 있습니다. **어떤 데이터가 표준 정규 분포(가우시안 분포)에 해당하도록 값을 바꿔준다**고 보면 될 것 같습니다. 그래서 데이터 X가 평균값과 같다면 0으로 정규화되겠지만, 평균보다 작으면 음수, 평균보다 크면 양수로 나타납니다.
> 
> 이때 계산되는 음수와 양수의 크기는 그 feature의 표준편차에 의해 결정됩니다. 그래서 만약 데이터의 표준편차가 크면, 즉 값이 넓게 퍼져있으면, 정규화되는 값이 0에 가까워집니다. 분모에 표준편차가 들어가고 분자에는 원래 데이터의 값에서 전체 데이터의 평균이 빠진다는 것을 유념하시면 이해가 빠를 것 같습니다.
> 
> ![](https://i.imgur.com/gfAtNb3.png)
> 
> 위 그림은 불균형한 데이터 분포를 평균이 0, 표준편차가 1이 되도록 변환한 것을 나타냅니다. 즉, 각각의 축으로 투영(projection)시켰을 때 정규분포가 되도록 한다는 것입니다.
> 

### 차원의 저주에 대해 설명해주세요. #⭐

차원의 저주는 말 그대로 데이터 차원이 늘어날수록 생기는 문제들을 말합니다. 차원이란 결국 feature 개수를 의미하는데, 예를 들어 이미지 하나가 50×50 픽셀이면 2,500차원이고, RGB로 보면 7,500차원이 됩니다.

문제는 차원이 늘어나면 공간이 기하급수적으로 커지면서 데이터가 희소해진다는 겁니다. 즉, 같은 데이터 개수로는 공간을 채우기가 어려워지고, 관측치 간 거리가 비슷해져서 유클리드 거리 같은 척도가 의미를 잃습니다. 이 때문에 k-NN 같은 거리 기반 알고리즘이 잘 안 먹히고, 모델은 노이즈까지 외워서 과적합할 위험이 커집니다. 또 계산량이 폭발적으로 늘어나고, 시각화나 해석도 힘들어집니다.

이걸 완화하는 방법은 차원 축소입니다. 대표적으로 **PCA**는 분산이 큰 축을 찾아 데이터를 압축하고, **LDA**는 클래스 구분이 잘 되게 축을 찾습니다. **t-SNE, UMAP** 같은 비선형 기법은 시각화에 많이 쓰이고요. 신경망 기반으로는 **오토인코더**가 있습니다. 또 불필요하거나 중복된 feature를 아예 제거하는 **특성 선택**도 좋은 방법입니다.

정리하면, 차원의 저주는 “차원이 많아질수록 데이터가 희소해지고 모델이 일반화하기 어려워진다”는 문제고, 해결책은 중요한 특징만 남기고 차원을 줄이는 겁니다.

> [!NOTE]- Detail
> #### 차원이란 무엇인가요?
> 데이터 분석 및 머신 러닝에서 차원은 데이터의 특징 또는 속성을 의미합니다. 예를 들어, 주택 데이터 세트를 고려할 때 차원은 주택의 가격, 크기, 침실 수, 위치 등을 포함할 수 있습니다. 
> 
> 머신러닝에서는 고차원 데이터를 자주 다룹니다. 쇼핑객마다 60개의 서로 다른 메트릭을 기록하는 경우 60차원 공간에서 작업하게 됩니다. 
> 
> 50x50 크기의 그레이스케일 이미지를 분석할 경우, 2,500차원 공간에서 작업하게 됩니다. 만약 이미지를 RGB 색상으로 처리하면 차원이 7,500으로 증가합니다. (각 픽셀의 각 색상 채널마다 한 차원).
> 
> ![](https://i.imgur.com/firCBHn.png)
> 
> ![](https://i.imgur.com/J33FrvN.png)
> 
> ![](https://i.imgur.com/LEmNrqU.png)
> 
> 
> #### 차원의 저주는 어떻게 발생하나요?
> 데이터 세트에 차원을 추가하면 공간의 부피가 기하급수적으로 증가합니다. 이는 데이터가 희소해진다는 것을 의미합니다. 예를 들어, 1차원에서는 몇 개의 점으로 쉽게 채울 수 있지만, 2차원에서는 더 많은 점이 필요합니다. 이제 3차원 큐브를 상상해보세요. 공간을 채우려면 더 많은 점이 필요합니다. 이 개념은 더 높은 차원으로 확장되어 데이터가 극도로 희소해집니다. 
> 
> 차원의 저주 (Curse of Dimensionality) 에는 두 가지 측면을 고려해야 합니다. 한편으로는 머신 러닝이 많은 차원을 가진 데이터를 분석하는 데 뛰어납니다. 인간은 많은 차원에 걸쳐 퍼져 있는 패턴을 찾는 데 능하지 않습니다. 특히 이러한 차원이 직관에 반하는 방식으로 상호 연관되어 있는 경우 더욱 그렇습니다. 다른 한편으로는 차원을 추가할수록 데이터를 분석하는 데 필요한 처리 능력과 의미 있는 데이터 모델을 만들기 위해 필요한 훈련 데이터 양도 증가합니다.
> 
> 고차원 데이터는 관측값(N)보다 특징 수(p)가 더 많은 데이터셋을 의미합니다. 고차원 데이터는 차원의 저주를 초래하는 문제입니다. 고차원 데이터의 방정식은 일반적으로 p >> N으로 표현됩니다.
> 
> **휴즈 현상 (Hughes Phenomenon)**
> 휴즈 현상(Hughes Phenomenon)은 특징 수가 증가함에 따라 분류기의 성능도 증가하다가 최적의 특징 수에 도달하면 성능이 최상에 이르는 현상을 나타냅니다. 그러나 동일한 크기의 훈련 세트를 기준으로 더 많은 특징을 추가하면 분류기의 성능이 저하됩니다.
> 
> ![](https://i.imgur.com/Oqivdrd.png)
> 
> #### 거리 함수 (Distance Function)
> 데이터 세트의 차원이 증가한다는 것은 해당하는 유클리드 공간에서 각 관찰을 나타내는 특징 벡터에 더 많은 항목이 추가된다는 것을 의미합니다. 벡터 공간에서 거리를 측정할 때 유클리드 거리를 사용합니다. 카르테시안 좌표를 가진 2차원 벡터 p = (p1, p2, ..., pn)와 q = (q1, q2, ..., qn) 사이의 유클리드 거리는 익숙한 공식을 사용하여 계산됩니다: 
> 
> ![](https://i.imgur.com/tK1Zey1.png)
> 
> 
> 따라서 각 새로운 차원이 합계에 음이 아닌 항을 추가하므로, 구별되는 벡터에 대해 차원이 증가할수록 거리는 증가합니다. 다시 말해, 주어진 관찰 수에 대해 특징 공간은 점점 더 희박해집니다;
> 
> 즉, 덜 밀집되거나 더 비게 됩니다. 반대로, 데이터 밀도가 낮아지면 평균 거리 유지를 위해 더 많은 관찰이 필요합니다. 관찰 간의 거리가 증가하면, 새로운 샘플에 대한 예측이 유사한 학습 특징에 기반할 가능성이 적어지므로 지도 학습이 더욱 어려워집니다. 가능한 고유 행의 수가 특징의 수가 증가할수록 기하급수적으로 증가하여, 효율적으로 일반화하기가 훨씬 더 어려워집니다. 
> 
> 더 많은 차원에서 노이즈에 과적합할 기회가 많아지면서 분산이 증가하여 일반화 성능이 저하됩니다. 실제로, 특징은 상관관계가 있거나 큰 변동을 나타내지 않는 경우가 많습니다. 이러한 이유로 차원 축소는 신호를 많이 잃지 않고 데이터를 압축하며, 메모리를 절약하면서 차원의 저주와 싸우는 데 도움이 됩니다.
> 
> ### 차원의 저주는 왜 발생하나요?
> 차원을 추가하면 데이터의 복잡성이 증가하지만 유용한 정보의 양이 반드시 증가하지는 않습니다. 또한, 고차원 공간에서는 대부분의 데이터 포인트가 "가장자리"나 "코너"에 위치하게 되어 데이터가 희소해집니다.
> 
> #### 차원의 저주는 어떤 문제를 일으키나요?
> - 데이터 희소성: 데이터가 희소해져 군집화 및 분류 작업이 어려워집니다.
> - 계산 증가: 차원이 늘어나면 데이터 처리에 더 많은 계산 자원과 시간이 필요합니다.
> - 과적합: 차원이 증가하면 모델이 과도하게 복잡해져 노이즈에 맞추게 됩니다. 이는 모델의 일반화 능력을 감소시킵니다.
> - 거리의 의미 상실: 고차원에서는 데이터 포인트 간 거리 차이가 거의 없어져 유클리드 거리 같은 측정이 의미를 잃습니다.
> - 성능 저하: k-최근접 이웃과 같은 거리 기반 알고리즘의 성능이 떨어질 수 있습니다.
> - 시각화 어려움: 고차원 데이터는 시각화가 어려워 탐색적 데이터 분석이 어려워집니다.
> 
> ### 차원의 저주는 어떻게 해결하나요?
> 차원의 저주를 해결하는 주요 방법은 "차원 축소"입니다. 차원 축소는 주요 변수 집합을 얻어 고려 중인 랜덤 변수를 줄이는 과정입니다. 차원을 줄임으로써 데이터에서 가장 중요한 정보를 유지하면서 중복되거나 덜 중요한 특징을 버릴 수 있습니다. 이를 통해 차원의 저주를 해결하는 방법은 다음과 같습니다:
> 
> - **주성분 분석(PCA)**: 데이터의 분산을 최대화하는 방향으로 새로운 축을 찾는 방법입니다. 고차원 데이터를 저차원으로 투영하여 중요한 정보를 보존합니다.
> - **선형 판별 분석(LDA)**: 클래스 간 분산을 최대화하고 클래스 내 분산을 최소화하는 방향을 찾습니다. 주로 분류 문제에서 사용됩니다.
> - **t-SNE 및 UMAP**: 데이터의 고차원 구조를 저차원 공간에 보존하면서 시각화를 돕는 비선형 차원 축소 기법입니다.
> - **특성 선택**: 중요도가 낮거나 중복되는 특성을 제거하여 차원을 줄입니다.
> - **오토인코더**: 신경망을 사용하여 입력 데이터를 압축하고 복원하는 방법입니다.
> 
> 이러한 기법들을 통해 데이터의 차원을 줄임으로써 차원의 저주 문제를 완화하고 분석 및 모델 성능을 향상시킬 수 있습니다.
> 
> 주성분 분석 (PCA, Principal Component Analysis)
> 
> 주성분 분석 (PCA)은 원래 변수들을 선형 결합으로 변환하여 새로운 변수 집합을 생성하는 통계 방법입니다. 이러한 새로운 변수는 주성분이라고 합니다. 예를 들어, 자동차의 마력, 토크, 가속도, 최고 속도 등의 다양한 측면에 대한 정보를 포함하는 데이터셋이 있다고 가정해 봅시다.
> 
> PCA를 사용하여 이 데이터셋의 차원을 축소하고자 합니다. PCA를 사용하면 주성분이라는 새로운 변수 집합을 만들 수 있습니다. 첫 번째 주성분은 데이터의 가장 큰 분산을 캡처하며, 이는 마력과 토크의 조합일 수 있습니다. 두 번째 주성분은 가속도와 최고 속도를 나타낼 수 있습니다. PCA를 통해 데이터의 차원을 축소함으로써 데이터셋을 보다 효과적으로 시각화하고 분석할 수 있습니다.
> 
> 선형 판별 분석(LDA, Linear Discriminant Analysis)
> 
> 선형 판별 분석 (LDA)는 클래스 간의 분산을 가장 잘 설명하는 속성을 식별하는 데 중점을 둡니다. 이는 특히 분류 작업에 유용합니다. 예를 들어, 꽃의 다양한 특성(예: 꽃잎 길이, 꽃잎 너비, 꽃받침 길이, 꽃받침 너비)을 포함하는 데이터셋이 있고, 각 꽃이 장미 또는 백합으로 레이블이 지정되어 있다고 가정해 봅시다. LDA를 사용하여 이 두 클래스 간의 분산을 가장 잘 설명하는 속성을 식별할 수 있습니다. LDA는 꽃잎 길이와 꽃잎 너비가 장미와 백합을 구분하는 데 가장 중요한 속성이라고 판단할 수 있습니다. 이러한 속성들의 선형 결합을 통해 새로운 변수를 생성하여 분류 작업에 사용할 수 있습니다. LDA를 통해 차원을 축소함으로써 꽃 분류 모델의 정확도를 향상시킬 수 있습니다.
> 
> t-분포 확률적 이웃 임베딩 (t-SNE, t-Distributed Stochastic Neighbor Embedding)
> 
> t-SNE는 고차원 데이터셋을 시각화하는 데 특히 유용한 비선형 차원 축소 기법입니다. 예를 들어, 고양이, 개, 새와 같은 다양한 동물의 이미지를 포함하는 데이터셋이 있다고 가정해 봅시다. 각 이미지는 심층 신경망에서 추출된 고차원 특징 벡터로 표현됩니다. 
> 
> t-SNE를 사용하여 이러한 특징 벡터의 차원을 2차원으로 줄이면 데이터셋을 시각화할 수 있습니다. t-SNE 알고리즘은 유사한 동물을 축소된 공간에서 더 가깝게 매핑하여 유사한 동물의 클러스터를 관찰할 수 있게 합니다. 이러한 시각화는 다양한 동물 유형 간의 관계와 유사성을 보다 직관적으로 이해하는 데 도움이 됩니다.
> 
> 
> 오토인코더 (Autoencoders)
> 
> 오토인코더 (Autoencoders)는 차원 축소를 위해 사용되는 신경망입니다. 입력 데이터를 압축된 표현으로 변환한 후, 이 표현에서 원래의 입력 데이터를 재구성합니다. 예를 들어, MNIST 데이터셋과 같은 손으로 쓴 숫자 이미지 데이터셋이 있다고 가정해 봅시다. 각 이미지는 고차원의 픽셀 벡터로 표현됩니다. 
> 
> 오토인코더를 사용하여 차원을 축소할 수 있습니다. 오토인코더는 입력 이미지를 저차원 표현(잠재 공간)으로 압축하는 법을 학습합니다. 이 잠재 공간은 이미지의 가장 중요한 특징을 캡처합니다. 그런 다음 오토인코더를 사용하여 잠재 공간 표현에서 원래 이미지를 재구성할 수 있습니다. 오토인코더를 사용하여 차원을 축소함으로써 이미지를 효과적으로 캡처하면서 불필요한 세부 정보를 제거할 수 있습니다.


#### PCA는 차원 축소 기법이면서, 데이터 압축 기법이기도 하고, 노이즈 제거기법이기도 합니다. 왜 그런지 설명해주실 수 있나요?

PCA는 데이터를 분산이 큰 방향으로 선형 변환해서 새로운 축, 즉 주성분을 만드는 기법입니다.

이 과정에서 차원을 줄이면 **차원 축소 기법**이 됩니다. 예를 들어 수천 개의 변수 대신 상위 몇 개 주성분만 쓰는 거죠.

또, 원래 데이터 대신 주성분으로 표현하면 필요한 정보가 압축되니까 **데이터 압축 기법**이기도 합니다. 원래 공간보다 훨씬 적은 수의 변수로도 데이터를 근사할 수 있습니다.

그리고 분산이 작은 축은 주로 노이즈 성분이기 때문에 버려지는 경우가 많습니다. 그래서 PCA는 **노이즈 제거 기법** 역할도 합니다. 본질적인 패턴은 유지하면서 불필요한 변동을 줄여주기 때문입니다.

정리하면, PCA는 **정보가 많이 담긴 축만 남겨 차원은 줄이고, 표현은 압축하고, 노이즈는 제거**하는 세 가지 효과를 동시에 낼 수 있는 방법입니다.

#### LSA, LDA, SVD 등의 약자들이 어떤 뜻이고 서로 어떤 관계를 가지는지 설명할 수 있나요?
텍스트와 관련된 자연어 처리와 정보 검색 분야에서 자주 사용되는 기술들

- **SVD**는 _Singular Value Decomposition_, 특이값 분해라고 합니다. 행렬을 세 개의 행렬로 분해해서 데이터의 구조를 파악하는 기초 수학 기법이에요.
- **LSA**는 _Latent Semantic Analysis_, 잠재 의미 분석입니다. 텍스트에서 단어와 문서 관계를 행렬로 표현한 뒤 SVD로 분해해서 숨겨진 의미 구조를 찾아내는 방법이에요.
- **LDA**는 두 가지가 있는데, 여기서는 보통 Latent Dirichlet Allocation을 말합니다. 확률적 생성 모델로, 문서가 여러 주제의 혼합으로 구성되어 있다고 가정하고 주제 분포를 추정하는 방법이에요. (통계 쪽 LDA는 Linear Discriminant Analysis지만, 텍스트 처리 맥락에서는 Latent Dirichlet Allocation을 많이 뜻합니다.)

관계를 정리하면, **SVD는 수학적 분해 기법**, **LSA는 그걸 텍스트에 적용한 방법**, **LDA는 확률 모델 기반 주제 추출 기법**입니다. 즉, LSA와 LDA는 둘 다 문서에서 주제를 찾는 데 쓰이고, LSA는 선형대수학적 접근, LDA는 확률론적 접근이라고 설명하면 깔끔합니다.


#### feature vector란 무엇일까요?
머신러닝 및 패턴 인식에서 사용되는 데이터 포인트를 나타내는 벡터

각각의 특성이 해당 벡터의 원소로 표현되며, 데이터 포인트의 특성들을 하나의 벡터로 결합한 것

이미지의 경우 각 픽셀을 하나의 특성으로 간주하고
텍스트 데이터의 경우 각 단어나 n-그램을 하나의 특성으로 간주



## 최적화와 수학적 기초

### Local Minima와 Global Minimum에 대해 설명해주세요. #⭐
비용 함수(cost function)에서의 **Global Minimum**은 에러가 최소화되는 즉, 우리가 찾고자 하는 지점을 말하며, **Local Minima**는 에러가 최소가 될 수 있는 후보가 되는 지점 중 Global Minimum을 뺀 지점을 말한다. 

Local Minima는 자칫 에러가 **최소화되는 지점을 찾았다고 착각**할 수 있기에 함정에 비유할 수 있다. 이를 해결하기 위해 Momentum과 같은 최적화 알고리즘을 사용하거나 학습률(learning rate)를 잘 조절하여 Local Minima에서 벗어날 수 있다.

### 최적화 기법 중 Newton’s Method와 Gradient Descent 방법에 대해 알고 있나요?

### L1, L2 정규화에 대해 설명해주세요. #⭐
정규화(**일반화**)는 모델이 학습할 때 가중치가 너무 커지지 않도록 패널티를 주는 기법이다.
즉 모델이 학습 데이터에 오버피팅되지 않고 처음 보는 테스트 데이터에도 좋은 성능을 내도록 만드는 것이다.

모델의 학습은 loss 함수를 최소화하는 방향으로 진행된다.

이 때, loss 함수에 L1, L2 정규화 항 (norm) 을 더함으로써 모델은 기존의 loss 도 줄이면서 정규화 항 (모델의 피쳐값과 관련) 도 줄이는 방향으로 학습된다.

모델의 피쳐값이 줄어듦에 따라 특정 피쳐가 너무 큰 값을 갖지 않게 되면서 오버피팅을 방지할 수 있게 된다.

L1 정규화 (라쏘 회귀)는 특정 피쳐의 값이 매우 낮은 경우 (아웃라이어) 0에 수렴되는 특징이 있다. 특정 피쳐가 0이 되어 사라지는 것은 **feature selection** 과 동일하다고 볼 수 있다.

L2 정규화 (릿지 회귀)는 특정 웨이트의 값이 매우 낮아도 0에 수렴되지는 않고 가까워지는 특징이 있다. 이는 L1 정규화에 비해 강하지 않게 정규화를 실행하여 항상 선형 모델에 일반화 효과를 줄 수 있다.

- 특성이 많고 해석 가능성이 중요하다 → **L1**
- 모든 특성을 활용하면서 안정적인 성능이 중요하다 → **L2**  
    그리고 둘을 적절히 섞은 **엘라스틱넷**도 실무에서 자주 쓰입니다.

### Cross Validation은 무엇이고 어떻게 해야하나요? #⭐
**cross validation(교차검증)이란** train(학습) 데이터로 학습한 모델이, 학습에 사용되지 않은 validation(검증) 데이터를 기준으로 얼마나 잘 동작하는지 확인하는 것이다. 여기서 주의할 점은 train 데이터셋과 validation 데이터셋에는 test 데이터셋이 포함되면 안된다는 것이다.

교차검증을 통해 얻을 수 있는 **장점**:
- 적은 데이터에 대한 validation 신뢰성을 높일 수 있다.
- 모든 데이터셋을 훈련에 활용할 수 있으므로 데이터 편중을 막을 수 있다. (k-fold 경우)
- 검증 결과에 따라 더 일반화된 모델을 만들 수 있다.
- 모델 학습에 오랜 시간이 소요된다.

교차검증 종류:
- 홀드 아웃 교차검증(Holdout Cross Validation)
- K-겹 교차검증(K-fold Cross Validation)
- 계층별 k-겹 교차검증(Stratified K-Fold Cross Validation)

#### 홀드 아웃 교차검증
홀드아웃 교차검증방법은 일정한 비율의 validation 데이터셋 하나를 지정하여 검증 데이터셋으로 사용하는 것이다. 홀드아웃 교차검증을 사용하는 경우, 두가지 문제점이 존재한다.

1. validation 데이터셋으로 지정된 부분의 데이터가 학습셋으로 사용되지 않는다는 문제
2. validation 데이터셋에 편향되도록 모델을 조정하게 된다는 문제

이를 해결하기 위해 k-fold 교차검증이 등장했다.

#### k-fold 교차검증
k-겹 교차검증 방법은 train 데이터를 k개의 fold로 나누어, 그 중 하나의 fold를 validation 데이터셋으로 삼아 검증하는 방법을 k번 반복하여, 그 평균을 결과로서 사용하는 방법이다. 세부적인 동작방법은 다음과 같다.

1. train 데이터셋을 k개의 fold로 나누고, 그 중 하나를 validation 데이터셋으로 지정한다.
2. validation 데이터셋을 제외한 나머지 폴드들을 train 데이터셋으로 사용하여 모델을 학습한다.
3. 학습한 모델을 1번에서 지정해둔 validation 데이터셋으로 검증하고, 그 검증 결과를 저장해둔다.
4. 모델을 초기화한 후, 기존 validation 데이터셋이 아닌 다른 fold를 validation 데이터셋으로 지정하고, 2번 과정부터 다시 수행한다.
5. 모든 fold들이 한번씩 validation 데이터셋으로 사용된 후에는, 저장해둔 검증결과의 평균을 내어, 그것을 최종 validation 결과로 사용한다.
    
그러나 k-겹 교차검증 방법은 랜덤하게 validation 데이터셋을 지정하게 되므로, 편향된 데이터로 이뤄진 폴드가 생성될 수 있다는 단점이 있다. 이를 해결하기 위해서 계층별 k-겹 교차검증 방법이 등장했다.

#### 계층별 k-겹 교차검증
계층별 k-겹 교차검증 방법은 k-겹 교차검증 방법에서 fold를 나눌때, 랜덤하게 fold를 지정하는 것이 아닌, 각 클래스별 비율을 고려하여 fold를 구성하는 방법이다.

#### 왜 test 데이터셋 만으로 검증하면 안될까?
모든 train 데이터셋을 학습하고, test 데이터셋으로 검증한 결과를 확인한다고 하자. 개발자는 test 데이터셋 점수를 높이기 위해, test 데이터셋에 편향되도록 모델을 튜닝하게 될 것이다. 그러나 중요한 것은 test 데이터셋에 대한 정확도를 높이는 것 뿐만아니라, 모델의 일반적인 정확도를 높이는 것이다. 어떤 데이터가 들어와도 일정하게 높은 정확도를 보여주는 모델이 좋은 모델이라 할 수 있으므로, validation 데이터셋과 test 데이터셋을 분리하여 검증하는 과정을 통해, 모델을 일반화시켜야 한다.

### OLS(ordinary least square) regression의 공식은 무엇인가요?

## 알고리즘

### SVM은 왜 반대로 차원을 확장시키는 방식으로 동작할까요? SVM은 왜 좋을까요?

### 다른 좋은 머신러닝 대비, 오래된 기법인 나이브 베이즈(naive bayes)의 장점을 옹호해보세요.

### Association Rule의 Support, Confidence, Lift에 대해 설명해주세요.

### K-means의 대표적 의미론적 단점은 무엇인가요? (계산량 많다는 것 말고)

### XGBoost을 아시나요? 왜 이 모델이 캐글에서 유명할까요? ⭐
**XGBoost(eXtreme Gradient Boosting)** 이란, 트리 기반의 앙상블 학습에서 가장 각광받고 있는 알고리즘 중 하나이다. Kaggle 경연대회에서 상위를 차지한 많은 과학자들이 XGBoost를 이용하면서 널리 알려졌다. GBM에 기반하고 있지만, GBM의 단점인 느린 수행시간 및 과적합 규제(Regularization) 부재 등의 문제를 해결해서 각광받고 있다.

XGBoost의 장점은 다음과 같다.

- 분류와 회귀영역에서 **뛰어난 예측 성능**을 발휘한다.
- XGBoost는 병렬처리를 사용하여, GBM 대비 **빠른 수행시간**을 보인다.
- **Regularization, Early Stopping** 기능을 통해 오버피팅을 방지할 수 있다.
- Tree Pruning(가지치기) 제공한다. 미리 정해둔 max_depth까지만 split하고 pruning을 하고, 거꾸로 올라가면서 positive gain이 없는 노드를 삭제한다.
- 자체적으로 결측치를 처리해준다.
- 매 iteration마다 교차검증을 수행한다.
    

**GBM(Gradient Boosting Algorithm)** 이란 회귀분석 또는 분류 분석을 수행할 수 있는 **예측모형**이며 예측모형의 **앙상블 방법론** 중 **부스팅** 계열에 속하는 알고리즘이다. LightGBM, CatBoost, XGBoost는 모두 GBM을 기반으로 만들어졌다. (자세한 내용은 [Gradient Boosting Algorithm의 직관적인 이해 - DeepPlay](https://3months.tistory.com/368) 참고)

> **💡 boosting 이라는 테크닉 자체가 sequential 한데 어떻게 병렬처리를 할까?** 세가지 가능성이 제기된다. 나뉜 분기마다 각각 병렬처리하거나, 분기가 나뉘는 지점 계산을 병렬처리 하거나, 처음부터 feature별 정렬을 통해 병렬처리를 할 수 있다. (자세한 내용은 [XGBoost의 병렬처리가 어떻게 가능할까? - GoLab](http://machinelearningkorea.com/2019/07/25/xgboost-%EC%9D%98-%EB%B3%91%EB%A0%AC%EC%B2%98%EB%A6%AC%EA%B0%80-%EC%96%B4%EB%96%BB%EA%B2%8C-%EA%B0%80%EB%8A%A5%ED%95%A0%EA%B9%8C/) 참고)


#### XGBoost는 트리를 어떤 방식으로 분기하는거죠?

### 앙상블 방법엔 어떤 것들이 있나요?

### 50개의 작은 의사결정 나무는 큰 의사결정 나무보다 괜찮을까요? 왜 그렇게 생각하나요?

## 제너럴

### 텍스트 더미에서 주제를 추출해야 합니다. 어떤 방식으로 접근해 나가시겠나요?

### Markov Chain을 고등학생에게 설명하려면 어떤 방식이 제일 좋을까요?

### 스팸 필터에 로지스틱 리그레션을 많이 사용하는 이유는 무엇일까요?

### 여러분이 서버를 100대 가지고 있습니다. 이때 인공신경망보다 Random Forest를 써야하는 이유는 뭘까요?

### 머신러닝(machine)적 접근방법과 통계(statistics)적 접근방법의 둘 간에 차이에 대한 견해가 있나요?

### 인공신경망(deep learning 이전의 전통적인)이 가지는 일반적인 문제점은 무엇일까요?

### 지금 나오고 있는 deep learning 계열의 혁신의 근간은 무엇이라고 생각하시나요?

### 좋은 모델의 정의는 무엇일까요?

---

## **Deep Learning**

### 기본 개념

#### 딥러닝은 무엇인가요? 딥러닝과 머신러닝의 차이는?

#### Cost Function과 Activation Function은 무엇인가요?

#### Tensorflow, PyTorch 특징과 차이가 뭘까요?

#### Data Normalization은 무엇이고 왜 필요한가요?

#### 알고있는 Activation Function에 대해 알려주세요. (Sigmoid, ReLU, LeakyReLU, Tanh 등)

#### 하이퍼 파라미터는 무엇인가요?

### 학습 안정화 및 초기화

#### 오버피팅일 경우 어떻게 대처해야 할까요?

#### Weight Initialization 방법에 대해 말해주세요. 그리고 무엇을 많이 사용하나요?

#### Adam 옵티마이저에 대해서 설명해주세요.

#### SGD, RMSprop, Adam에 대해서 아는대로 설명한다면?

##### SGD에서 Stochastic의 의미는?

##### 미니배치를 작게 할 때의 장단점은?

##### 모멘텀의 수식을 적어 본다면?

### Activation Function 심화

#### 요즘 Sigmoid 보다 ReLU를 많이 쓰는데 그 이유는?

##### Non-Linearity라는 말의 의미와 그 필요성은?

##### ReLU로 어떻게 곡선 함수를 근사하나?

##### ReLU의 문제점은?

##### Bias는 왜 있는걸까?

### 최적화와 학습 과정

#### Gradient Descent에 대해서 쉽게 설명한다면?

##### 왜 꼭 Gradient를 써야 할까? 그 그래프에서 가로축과 세로축 각각은 무엇인가? 실제 상황에서는 그 그래프가 어떻게 그려질까?

##### GD 중에 때때로 Loss가 증가하는 이유는?

##### Back Propagation에 대해서 쉽게 설명 한다면?

#### Local Minima 문제에도 불구하고 딥러닝이 잘 되는 이유는?

##### GD가 Local Minima 문제를 피하는 방법은?

##### 찾은 해가 Global Minimum인지 아닌지 알 수 있는 방법은?

### 데이터 분리 및 일반화

#### Training 세트와 Test 세트를 분리하는 이유는?

##### Validation 세트가 따로 있는 이유는?

##### Test 세트가 오염되었다는 말의 뜻은?

##### Regularization이란 무엇인가?

### 학습 안정화 기법

#### Batch Normalization의 효과는?

##### Dropout의 효과는?

##### BN 적용해서 학습 이후 실제 사용시에 주의할 점은? 코드로는?

##### GAN에서 Generator 쪽에도 BN을 적용해도 될까?

### 구현 및 실습

#### 간단한 MNIST 분류기를 MLP+CPU 버전으로 numpy로 만든다면 몇 줄일까?

##### 어느 정도 돌아가는 녀석을 작성하기까지 몇 시간 정도 걸릴까?

##### Back Propagation은 몇 줄인가?

##### CNN으로 바꾼다면 얼마나 추가될까?

#### 간단한 MNIST 분류기를 TF, PyTorch 등으로 작성하는데 몇 시간 필요한가?

##### CNN이 아닌 MLP로 해도 잘 될까?

##### 마지막 레이어 부분에 대해서 설명 한다면?

##### 학습은 BCE loss로 하되 상황을 MSE loss로 보고 싶다면?

### 하드웨어와 성능 최적화

#### 딥러닝할 때 GPU를 쓰면 좋은 이유는?

##### GPU를 두 개 다 쓰고 싶다. 방법은?

##### 학습 시 필요한 GPU 메모리는 어떻게 계산하는가?

### 기타 개념

#### 볼츠만 머신은 무엇인가요?

#### 뉴럴넷의 가장 큰 단점은 무엇인가? 이를 위해 나온 One-Shot Learning은 무엇인가?

#### TF, PyTorch 등을 사용할 때 디버깅 노하우는?

#### Augmentation을 어떤 식으로 해야 되는지, 사용 전후 결과가 어떤 식으로 달라졌는지 설명해주세요.



---
## **Stats / Math**

### 선형대수 및 기초 개념

#### 고유값(eigen value)와 고유벡터(eigen vector)이 무엇이고 왜 중요한지 설명해주세요.

### 확률 및 분포

#### 확률 모형과 확률 변수는 무엇인가요?

#### 누적 분포 함수와 확률 밀도 함수는 무엇인가요? 수식과 함께 표현해주세요.

#### 조건부 확률은 무엇인가요?

#### 베르누이 분포 / 이항 분포 / 카테고리 분포 / 다항 분포 / 가우시안 정규 분포 / t 분포 / 카이제곱 분포 / F 분포 / 베타 분포 / 감마 분포에 대해 설명해주세요. 그리고 분포 간의 연관성도 설명해주세요.

### 통계적 추정과 검정

#### 샘플링(Sampling)과 리샘플링(Resampling)이 무엇이고 리샘플링의 장점을 말씀해주세요.

#### 신뢰 구간의 정의는 무엇인가요?

#### p-value를 모르는 사람에게 설명한다면 어떻게 설명하실 건가요?

#### R square의 의미는 무엇인가요?

#### 중심극한정리는 왜 유용한걸까요?

#### “likelihood”와 “probability”의 차이는 무엇일까요?

#### 통계에서 사용되는 bootstrap의 의미는 무엇인가요.

#### 검정력(statistical power)은 무엇일까요?

#### 필요한 표본의 크기를 어떻게 계산합니까?

### 통계적 방법론

#### 어떨 때 모수적 방법론을 쓸 수 있고, 어떨 때 비모수적 방법론을 쓸 수 있나요?

#### 베이지안과 프리퀀티스트 간의 입장차이를 설명해주실 수 있나요?

### 데이터 특성과 처리

#### 평균(mean)과 중앙값(median) 중에 어떤 케이스에서 뭐를 써야할까요?

#### 공분산과 상관계수는 무엇일까요? 수식과 함께 표현해주세요.

#### 엔트로피(entropy)에 대해 설명해주세요. 가능하면 Information Gain도요.

#### missing value가 있을 경우 채워야 할까요? 그 이유는 무엇인가요?

#### 아웃라이어를 판단하는 기준은 무엇인가요?

#### Bias를 통제하는 방법은 무엇입니까?

#### 모수가 매우 적은 (수십개 이하) 케이스의 경우 어떤 방식으로 예측 모델을 수립할 수 있을까요?

### 수학적 함수와 응용

#### 로그 함수는 어떤 경우 유용합니까? 사례를 들어 설명해주세요.

### 확률 퍼즐

#### 출장을 위해 비행기를 타려고 합니다. 당신은 우산을 가져가야 하는지 알고 싶어 출장지에 사는 친구 3명에게 무작위로 전화를 하고 비가 오는 경우를 독립적으로 질문해주세요. 각 친구는 2/3로 진실을 말하고 1/3으로 거짓을 말합니다. 3명의 친구가 모두 “그렇습니다. 비가 내리고 있습니다”라고 말했습니다. 실제로 비가 내릴 확률은 얼마입니까?


---
## Reference
https://github.com/zzsza/Datascience-Interview-Questions

https://ineed-coffee.github.io/posts/job-interview-review/

https://ploradoaa.tistory.com/91

https://discuss.pytorch.kr/

https://mole-starseeker.tistory.com/category/%EB%94%A5%EB%9F%AC%EB%8B%9D%20%26%20%EB%A8%B8%EC%8B%A0%EB%9F%AC%EB%8B%9D/%EB%94%A5%EB%9F%AC%EB%8B%9D%20%EC%A7%80%EC%8B%9D

https://mole-starseeker.tistory.com/31

https://modulabs.co.kr/blog/%EC%B0%A8%EC%9B%90%EC%9D%98-%EC%A0%80%EC%A3%BC-curse-of-dimensionality

https://zzangyeah.tistory.com/category/%EA%B3%B5%EB%B6%80/ML%20%EB%AA%A8%EC%9D%98%20%EC%9D%B8%ED%84%B0%EB%B7%B0%20%EC%8A%A4%ED%84%B0%EB%94%94

https://zzangyeah.tistory.com/237